import React, { useState } from 'react';
import { TouchableOpacity, Text, StyleSheet } from 'react-native';

const PesquisaDespesa = ({ navigation }) => {

    return (
        <Text>Tela de Pesquisa</Text>
    )
}

export default PesquisaDespesa;